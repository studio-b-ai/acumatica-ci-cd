<?xml version="1.0" encoding="utf-8"?>
<!--
  ╔══════════════════════════════════════════════════════════════════════════════╗
  ║  PricingBreakPanel.xml                                                     ║
  ║  Heritage Fabrics / Studio B  ·  Acumatica 2024 R2 (24.208)               ║
  ║                                                                            ║
  ║  Customization project metadata for the Break Pricing panel on SO301000.  ║
  ║                                                                            ║
  ║  PROJECT CONTENTS                                                          ║
  ║  ─────────────────────────────────────────────────────────────────────────  ║
  ║  1. Page delta     — SO301000.aspx: PXSmartPanel + PXGroupBox + PXGrid    ║
  ║  2. DAC            — SOPricingBreakHeader  (unbound filter, header label)  ║
  ║  3. DAC            — SOPricingBreakTier    (PXVirtual, grid rows)         ║
  ║  4. Graph ext.     — SOOrderEntry_BreakPricing (view delegate + events)   ║
  ║                                                                            ║
  ║  DEPLOYMENT                                                                ║
  ║  ─────────────────────────────────────────────────────────────────────────  ║
  ║  Upload to Acumatica via SM204505 (Customization Projects).                ║
  ║  Publish at level 0 — no schema changes, no SQL scripts required.         ║
  ║  The graph extension reads from existing ARSalesPrice (base-price) rows;  ║
  ║  the virtual DACs (PXVirtual / PXFilter) carry no DB footprint.           ║
  ║                                                                            ║
  ║  RELATED FILES                                                             ║
  ║  ─────────────────────────────────────────────────────────────────────────  ║
  ║  acumatica/customization/SOOrderEntryExt.aspx  — human-readable ASPX ref  ║
  ║  Customization/_project/Code/DAC/SOPricingBreakHeader.cs                  ║
  ║  Customization/_project/Code/DAC/SOPricingBreakTier.cs                    ║
  ║  Customization/_project/Code/Graph/SOOrderEntry_BreakPricing.cs           ║
  ║  Customization/_project/PageLayout/SO301000_BreakPricingPanel.xml         ║
  ╚══════════════════════════════════════════════════════════════════════════════╝
-->
<Customization
    level="0"
    description="Adds break pricing display panel to Sales Order screen"
    product-version="24.208">

    <!--
      ════════════════════════════════════════════════════════════════════════════
      DAC 1: SOPricingBreakHeader
      ────────────────────────────────────────────────────────────────────────────
      Unbound single-row DAC used as the PXFilter data source for the panel header
      label (PXFormView ID="frmBreakPricingHeader").

      Fields:
        ItemDescription  — display string "{InventoryCD} – {Descr}", computed by
                           SOOrderEntry_BreakPricing.RowSelected<SOLine> event.
        InventoryID      — backing field used by graph extension to short-circuit
                           re-loading when the same item is focused again.

      [PXCacheName("Break Pricing Header")]
      No DB table — purely virtual / filter.
      ════════════════════════════════════════════════════════════════════════════
    -->
    <Graph ClassName="SOPricingBreakHeader" Source="#CDATA" IsNew="True" FileType="NewFile">
        <CDATA name="Source"><![CDATA[using System;
using PX.Data;
using PX.Data.BQL;

namespace HeritageFabrics.SO
{
    /// <summary>
    /// Unbound single-row DAC used as the data source for the PXFormView header inside
    /// the Break Pricing panel on SO301000.
    ///
    /// Why a separate DAC instead of putting the label on SOPricingBreakTier?
    ///   • PXFormView (the header) and PXGrid (the tiers) must bind to *different*
    ///     data members.  Acumatica does not allow a single DAC/view to serve both.
    ///   • A PXFilter<SOPricingBreakHeader> view is always current (single row),
    ///     which is exactly what a header label needs.
    ///
    /// The graph extension (SOOrderEntry_BreakPricing) keeps this current by calling
    ///   BreakPricingHeader.Current.ItemDescription = ...
    /// whenever the selected SOLine changes.
    /// </summary>
    [Serializable]
    [PXCacheName("Break Pricing Header")]
    public class SOPricingBreakHeader : IBqlTable
    {
        #region ItemDescription
        /// <summary>
        /// Formatted string displayed in the panel header label.
        /// Built by the graph extension as:  "{InventoryCD} – {Descr}"
        /// Falls back to "(no item selected)" when no line is active.
        /// </summary>
        public abstract class itemDescription : BqlString.Field<itemDescription> { }

        [PXString(512, IsUnicode = true)]
        [PXUIField(DisplayName = "Item", Enabled = false)]
        public virtual string ItemDescription { get; set; }
        #endregion

        #region InventoryID (backing field — not displayed)
        /// <summary>
        /// Tracks which InventoryID is currently reflected in the header and grid.
        /// Used by the graph extension to short-circuit re-loading when the user
        /// clicks a different split of the same item.
        /// </summary>
        public abstract class inventoryID : BqlInt.Field<inventoryID> { }

        [PXInt]
        [PXUIField(DisplayName = "Inventory ID", Visible = false, Enabled = false)]
        public virtual int? InventoryID { get; set; }
        #endregion
    }
}]]></CDATA>
    </Graph>

    <!--
      ════════════════════════════════════════════════════════════════════════════
      DAC 2: SOPricingBreakTier
      ────────────────────────────────────────────────────────────────────────────
      Virtual (unbound) DAC used as the view-model for the break-tier grid rows.
      Populated in-memory by SOOrderEntry_BreakPricing from ARSalesPrice records.

      Fields:
        TierOrder    [IsKey]  — 1-based display sequence / cache identity key
        MinQty               — break lower bound (ARSalesPrice.BreakQty)
        MaxQty               — break upper bound (next tier's BreakQty, or null)
        UnitPrice            — price at this tier (ARSalesPrice.SalesPrice)
        UOM                  — unit of measure (ARSalesPrice.UOM)
        InventoryID  [hidden] — cache correlation, not displayed

      [PXVirtual] — ephemeral cache, never persisted to the database.
      ════════════════════════════════════════════════════════════════════════════
    -->
    <Graph ClassName="SOPricingBreakTier" Source="#CDATA" IsNew="True" FileType="NewFile">
        <CDATA name="Source"><![CDATA[using System;
using PX.Data;
using PX.Data.BQL;
using PX.Objects.IN;

namespace HeritageFabrics.SO
{
    /// <summary>
    /// Unbound/projection DAC used exclusively as a view-model for the Break Pricing
    /// panel on SO301000.  Rows are populated in-memory by SOOrderEntry_BreakPricing
    /// by reading ARSalesPrice break-tier records for the inventory item that is
    /// currently selected in the Document Details grid.
    ///
    /// Why unbound?
    ///   • Break-price records already live in standard Acumatica tables (ARSalesPrice).
    ///   • We need a *filtered, display-only* projection — no additional DB table.
    ///   • PXVirtualDAC keeps the cache ephemeral and never triggers a persist.
    ///
    /// Column layout matches the panel grid:
    ///   Tier | Min Qty | Max Qty | Unit Price | UOM
    /// </summary>
    [Serializable]
    [PXCacheName("Break Pricing Tier")]
    [PXVirtual]
    public class SOPricingBreakTier : IBqlTable
    {
        #region TierOrder
        /// <summary>
        /// Display sequence (1-based). Drives grid sort order.
        /// This is the key field — the cache uses it as the identity for each row.
        /// </summary>
        public abstract class tierOrder : BqlInt.Field<tierOrder> { }

        [PXInt(IsKey = true)]
        [PXUIField(DisplayName = "Tier", Enabled = false)]
        public virtual int? TierOrder { get; set; }
        #endregion

        #region MinQty
        /// <summary>
        /// Minimum quantity (break-price lower bound), inclusive.
        /// Sourced from ARSalesPrice.BreakQty.
        /// </summary>
        public abstract class minQty : BqlDecimal.Field<minQty> { }

        [PXDecimal(4)]
        [PXUIField(DisplayName = "Min Qty", Enabled = false)]
        public virtual decimal? MinQty { get; set; }
        #endregion

        #region MaxQty
        /// <summary>
        /// Maximum quantity (break-price upper bound), exclusive.
        /// Derived as the MinQty of the next tier; null for the last tier.
        /// Displayed as "No Limit" in the grid via NullText="No Limit" on the column.
        /// </summary>
        public abstract class maxQty : BqlDecimal.Field<maxQty> { }

        [PXDecimal(4)]
        [PXUIField(DisplayName = "Max Qty", Enabled = false)]
        public virtual decimal? MaxQty { get; set; }
        #endregion

        #region UnitPrice
        /// <summary>
        /// Price per unit of measure at this tier, in the order's currency.
        /// Sourced from ARSalesPrice.SalesPrice.
        /// Rendered with Format="Currency" in the grid column.
        /// </summary>
        public abstract class unitPrice : BqlDecimal.Field<unitPrice> { }

        [PXDecimal(4)]
        [PXUIField(DisplayName = "Unit Price", Enabled = false)]
        public virtual decimal? UnitPrice { get; set; }
        #endregion

        #region UOM
        /// <summary>
        /// Unit of measure for MinQty / MaxQty quantities and UnitPrice.
        /// Sourced from ARSalesPrice.UOM.
        /// </summary>
        public abstract class uOM : BqlString.Field<uOM> { }

        [PXString(6, IsUnicode = true)]
        [PXUIField(DisplayName = "UOM", Enabled = false)]
        public virtual string UOM { get; set; }
        #endregion

        #region InventoryID (hidden — cache correlation only)
        /// <summary>
        /// InventoryID of the currently-selected SO line item.
        /// Not displayed in the grid; used by the graph extension to invalidate
        /// the cache when the user selects a different line item.
        /// </summary>
        public abstract class inventoryID : BqlInt.Field<inventoryID> { }

        [PXInt]
        [PXUIField(DisplayName = "Inventory ID", Visible = false, Enabled = false)]
        public virtual int? InventoryID { get; set; }
        #endregion
    }
}]]></CDATA>
    </Graph>

    <!--
      ════════════════════════════════════════════════════════════════════════════
      Graph Extension: SOOrderEntry_BreakPricing
      ────────────────────────────────────────────────────────────────────────────
      PXGraphExtension<SOOrderEntry> that:
        • Exposes PXFilter<SOPricingBreakHeader>     → view "BreakPricingHeader"
        • Exposes PXSelectReadonly<SOPricingBreakTier>  → view "BreakPricingTiers"
        • Implements breakPricingTiers() delegate to build tier rows in-memory
          from ARSalesPrice (PriceType='B', non-promotional, ordered by BreakQty)
        • Handles RowSelected<SOLine> to refresh both views when the focused
          Document Details line changes
        • Derives MaxQty per tier as the next tier's BreakQty (null for last)
      ════════════════════════════════════════════════════════════════════════════
    -->
    <Graph ClassName="SOOrderEntry_BreakPricing" Source="#CDATA" IsNew="True" FileType="NewFile">
        <CDATA name="Source"><![CDATA[using System;
using System.Collections;
using System.Collections.Generic;
using PX.Data;
using PX.Data.BQL;
using PX.Data.BQL.Fluent;
using PX.Objects.AR;
using PX.Objects.IN;
using PX.Objects.SO;

namespace HeritageFabrics.SO
{
    /// <summary>
    /// Graph extension for SOOrderEntry that drives the Break Pricing panel on SO301000.
    ///
    /// Responsibilities
    /// ────────────────
    /// 1. Expose a PXFilter&lt;SOPricingBreakHeader&gt; view (header label data).
    /// 2. Expose a PXSelectReadonly&lt;SOPricingBreakTier&gt; virtual delegate view (grid rows).
    /// 3. React to SOLine row selection changes (RowSelected) and refresh both views.
    /// 4. Query ARSalesPrice break tiers for the selected inventory item, sorted by qty.
    /// 5. Derive MaxQty as the next tier's BreakQty; null for the last tier.
    ///
    /// Data source: ARSalesPrice
    ///   • Filtered by: InventoryID, PriceType = 'B' (base price),
    ///                  IsPromotionalPrice = false
    ///   • Ordered by BreakQty ASC
    ///   • This gives the full break-price ladder regardless of the specific
    ///     customer on the order — appropriate for a visual reference panel.
    ///
    /// Acumatica version target: 2024 R2 (24.208)
    /// </summary>
    public class SOOrderEntry_BreakPricing : PXGraphExtension<SOOrderEntry>
    {
        public static bool IsActive() => true;

        // ─────────────────────────────────────────────────────────────────────
        // Views
        // ─────────────────────────────────────────────────────────────────────

        /// <summary>
        /// Single-row filter driving the panel header label.
        /// Bind the PXFormView on SO301000 to DataMember="BreakPricingHeader".
        /// </summary>
        public PXFilter<SOPricingBreakHeader> BreakPricingHeader;

        /// <summary>
        /// Delegate-backed virtual view returning break tier rows for the
        /// currently-selected SOLine's InventoryID.
        /// Bind the nested PXGrid on SO301000 to DataMember="BreakPricingTiers".
        /// </summary>
        public PXSelectReadonly<SOPricingBreakTier> BreakPricingTiers;

        /// <summary>
        /// Custom delegate for BreakPricingTiers.  Acumatica calls this when the
        /// grid needs to populate.  Rows are built in-memory from ARSalesPrice.
        /// </summary>
        protected virtual IEnumerable breakPricingTiers()
        {
            SOPricingBreakHeader header = BreakPricingHeader.Current;
            if (header?.InventoryID == null)
                yield break;

            foreach (SOPricingBreakTier tier in LoadTiers(header.InventoryID.Value))
                yield return tier;
        }

        // ─────────────────────────────────────────────────────────────────────
        // Event Handlers
        // ─────────────────────────────────────────────────────────────────────

        /// <summary>
        /// Fires whenever a row in the Document Details grid is highlighted.
        /// Refreshes the break pricing panel to match the focused SOLine.
        /// </summary>
        protected void _(Events.RowSelected<SOLine> e)
        {
            SOLine line = e.Row;
            SOPricingBreakHeader header = BreakPricingHeader.Current
                ?? new SOPricingBreakHeader();

            // Short-circuit: same item as before — nothing to reload
            if (line?.InventoryID != null && line.InventoryID == header.InventoryID)
                return;

            if (line?.InventoryID == null)
            {
                // No line selected — clear the panel
                header.InventoryID = null;
                header.ItemDescription = Messages.NoItemSelected;
            }
            else
            {
                int inventoryID = line.InventoryID.Value;
                header.InventoryID    = inventoryID;
                header.ItemDescription = ResolveItemDescription(inventoryID);
            }

            BreakPricingHeader.Cache.Clear();
            BreakPricingHeader.Cache.Insert(header);

            // Force the tier grid to re-query on next repaint
            BreakPricingTiers.Cache.Clear();
        }

        // ─────────────────────────────────────────────────────────────────────
        // Core data-loading helpers
        // ─────────────────────────────────────────────────────────────────────

        /// <summary>
        /// Queries ARSalesPrice for all base-price break tiers for the given
        /// InventoryID (PriceType = 'B', non-promotional), ordered by BreakQty
        /// ascending, then builds SOPricingBreakTier view-model objects with
        /// derived MaxQty values.
        /// </summary>
        private List<SOPricingBreakTier> LoadTiers(int inventoryID)
        {
            var rawRows = SelectFrom<ARSalesPrice>
                .Where<ARSalesPrice.inventoryID.IsEqual<@P.AsInt>
                    .And<ARSalesPrice.priceType.IsEqual<ARPriceType.basePrice>>
                    .And<ARSalesPrice.isPromotionalPrice.IsEqual<False>>>
                .OrderBy<ARSalesPrice.breakQty.Asc>
                .View.ReadOnly.Select(Base, inventoryID);

            var priceList = new List<ARSalesPrice>();
            foreach (PXResult<ARSalesPrice> row in rawRows)
                priceList.Add((ARSalesPrice)row);

            var tiers = new List<SOPricingBreakTier>();
            for (int i = 0; i < priceList.Count; i++)
            {
                ARSalesPrice price = priceList[i];

                // MaxQty = next tier's BreakQty; null for the last tier
                // (rendered as "No Limit" by NullText on the grid column)
                decimal? maxQty = (i + 1 < priceList.Count)
                    ? priceList[i + 1].BreakQty
                    : (decimal?)null;

                tiers.Add(new SOPricingBreakTier
                {
                    TierOrder   = i + 1,
                    MinQty      = price.BreakQty,
                    MaxQty      = maxQty,
                    UnitPrice   = price.SalesPrice,
                    UOM         = price.UOM,
                    InventoryID = inventoryID,
                });
            }

            return tiers;
        }

        /// <summary>
        /// Returns a display string "SKU – Description" for the panel header label.
        /// Falls back gracefully if the item record is not found.
        /// </summary>
        private string ResolveItemDescription(int inventoryID)
        {
            InventoryItem item = SelectFrom<InventoryItem>
                .Where<InventoryItem.inventoryID.IsEqual<@P.AsInt>>
                .View.ReadOnly.SelectSingleBound(Base, null, inventoryID);

            if (item == null)
                return Messages.NoItemSelected;

            string cd    = item.InventoryCD?.Trim() ?? string.Empty;
            string descr = item.Descr?.Trim()        ?? string.Empty;

            if (string.IsNullOrEmpty(descr)) return cd;
            return string.IsNullOrEmpty(cd) ? descr : $"{cd} \u2013 {descr}";
        }

        // ─────────────────────────────────────────────────────────────────────
        // String constants
        // ─────────────────────────────────────────────────────────────────────
        private static class Messages
        {
            public const string NoItemSelected = "(no item selected)";
        }
    }
}]]></CDATA>
    </Graph>

    <!--
      ════════════════════════════════════════════════════════════════════════════
      Page Delta: SO301000.aspx
      ────────────────────────────────────────────────────────────────────────────
      Instructs Acumatica's page merger to inject the PXSmartPanel above the
      tab strip (ID="tab") that contains the Document Details grid.

      Control placement:
        ParentId="phG_tab"  — the content placeholder that wraps both the
                              header FormView area and the PXTab strip
        InsertBefore="tab"  — positions our panel before the PXTab, i.e. above
                              the Document Details grid, below the header form

      The SmartPanel:
        Key="BreakPricingHeader"   — scopes auto-repaint to the header filter view
        AutoRepaint="True"         — enables automatic re-render on view change

      The GroupBox:
        Caption              = "Break Pricing for Selected Item"
        RenderStyle          = Fieldset   → native Acumatica collapsible fieldset
        CollapsibleBehavior  = ServerSide → state persisted across postbacks
        InitialExpandState   = Expanded   → panel open by default

      The Grid Mode:
        AllowAddNew="False"  ─┐
        AllowDelete="False"   ├─ fully read-only (display reference panel only)
        AllowUpdate="False"  ─┘

      Grid Columns (all bound to SOPricingBreakTier DAC fields):
        TierOrder  50px  Center           — "Tier"
        MinQty     80px  Right            — "Min Qty"
        MaxQty     80px  Right NullText   — "Max Qty" / "No Limit"
        UnitPrice  90px  Right Currency   — "Unit Price"
        UOM        60px                   — "UOM"
      ════════════════════════════════════════════════════════════════════════════
    -->
    <Page path="~/pages/so/so301000.aspx">

        <!--
          ── Outer auto-repainting smart panel ──────────────────────────────────
          InsertBefore="tab" places the panel directly above the PXTab (ID="tab")
          that hosts the Document Details grid.  This is the canonical Acumatica
          2023R2+ pattern for inserting panels between the header and detail tabs.
        -->
        <PXSmartPanel
            ID="pnlBreakPricing"
            ParentId="phG_tab"
            InsertBefore="tab"
            TypeFullName="PX.Web.UI.PXSmartPanel">
            <Prop Key="Key"          Value="BreakPricingHeader" />
            <Prop Key="AutoRepaint"  Value="True" />
            <Prop Key="style"        Value="margin-bottom:6px;" />

            <Children Key="ContentTemplate">

                <!-- Collapsible GroupBox wrapping the entire panel content -->
                <AddItem>
                    <PXGroupBox
                        ID="gbBreakPricing"
                        TypeFullName="PX.Web.UI.PXGroupBox">
                        <Prop Key="Caption"               Value="Break Pricing for Selected Item" />
                        <Prop Key="DataMember"            Value="BreakPricingHeader" />
                        <Prop Key="DataSourceID"          Value="ds" />
                        <Prop Key="RenderStyle"           Value="Fieldset" />
                        <Prop Key="CollapsibleBehavior"   Value="ServerSide" />
                        <Prop Key="InitialExpandState"    Value="Expanded" />
                        <Prop Key="ScreenID"              Value="SO301000" />
                        <Prop Key="style"                 Value="margin:0 0 6px 0; width:100%;" />

                        <Children Key="Template">

                            <!-- Header FormView — item label above the grid -->
                            <AddItem>
                                <PXFormView
                                    ID="frmBreakPricingHeader"
                                    TypeFullName="PX.Web.UI.PXFormView">
                                    <Prop Key="DataMember"   Value="BreakPricingHeader" />
                                    <Prop Key="DataSourceID" Value="ds" />
                                    <Prop Key="SkinID"       Value="Transparent" />
                                    <Prop Key="TabIndex"     Value="1800" />

                                    <Children Key="Template">
                                        <AddItem>
                                            <PXLayoutRule TypeFullName="PX.Web.UI.PXLayoutRule">
                                                <Prop Key="StartColumn" Value="True" />
                                                <Prop Key="LabelsWidth" Value="S" />
                                                <Prop Key="ControlSize" Value="XL" />
                                            </PXLayoutRule>
                                        </AddItem>
                                        <!-- Bound to SOPricingBreakHeader.ItemDescription -->
                                        <AddItem>
                                            <PXTextEdit
                                                ID="edBreakPricingItemDesc"
                                                TypeFullName="PX.Web.UI.PXTextEdit">
                                                <Prop Key="DataField"  Value="ItemDescription" />
                                                <Prop Key="LabelWidth" Value="120px" />
                                                <Prop Key="Size"       Value="XL" />
                                                <Prop Key="Enabled"    Value="False" />
                                            </PXTextEdit>
                                        </AddItem>
                                    </Children>
                                </PXFormView>
                            </AddItem>

                            <!-- Read-only break-tier grid -->
                            <AddItem>
                                <PXGrid
                                    ID="gridBreakTiers"
                                    TypeFullName="PX.Web.UI.PXGrid">
                                    <Prop Key="DataMember"     Value="BreakPricingTiers" />
                                    <Prop Key="DataSourceID"   Value="ds" />
                                    <Prop Key="SkinID"         Value="ShortList" />
                                    <Prop Key="Height"         Value="120px" />
                                    <Prop Key="Width"          Value="100%" />
                                    <Prop Key="AllowPaging"    Value="False" />
                                    <Prop Key="AdjustPageSize" Value="Auto" />
                                    <Prop Key="NoteIndicator"  Value="False" />
                                    <Prop Key="FilesIndicator" Value="False" />
                                    <Prop Key="TabIndex"       Value="1810" />

                                    <!-- Fully read-only mode -->
                                    <Children Key="Mode">
                                        <AddItem>
                                            <PXGridMode TypeFullName="PX.Web.UI.PXGridMode">
                                                <Prop Key="AllowAddNew" Value="False" />
                                                <Prop Key="AllowDelete" Value="False" />
                                                <Prop Key="AllowUpdate" Value="False" />
                                            </PXGridMode>
                                        </AddItem>
                                    </Children>

                                    <Children Key="Levels">
                                        <AddItem>
                                            <PXGridLevel
                                                DataMember="BreakPricingTiers"
                                                TypeFullName="PX.Web.UI.PXGridLevel">
                                                <Children Key="Columns">

                                                    <!-- Col 1: Tier (1-based sequence) -->
                                                    <AddItem>
                                                        <PXGridColumn TypeFullName="PX.Web.UI.PXGridColumn">
                                                            <Prop Key="DataField"   Value="TierOrder" />
                                                            <Prop Key="Width"       Value="50px" />
                                                            <Prop Key="TextAlign"   Value="Center" />
                                                            <Prop Key="HeaderText"  Value="Tier" />
                                                        </PXGridColumn>
                                                    </AddItem>

                                                    <!-- Col 2: Min Qty -->
                                                    <AddItem>
                                                        <PXGridColumn TypeFullName="PX.Web.UI.PXGridColumn">
                                                            <Prop Key="DataField"   Value="MinQty" />
                                                            <Prop Key="Width"       Value="80px" />
                                                            <Prop Key="TextAlign"   Value="Right" />
                                                            <Prop Key="HeaderText"  Value="Min Qty" />
                                                        </PXGridColumn>
                                                    </AddItem>

                                                    <!-- Col 3: Max Qty ("No Limit" when null = last tier) -->
                                                    <AddItem>
                                                        <PXGridColumn TypeFullName="PX.Web.UI.PXGridColumn">
                                                            <Prop Key="DataField"   Value="MaxQty" />
                                                            <Prop Key="Width"       Value="80px" />
                                                            <Prop Key="TextAlign"   Value="Right" />
                                                            <Prop Key="HeaderText"  Value="Max Qty" />
                                                            <Prop Key="NullText"    Value="No Limit" />
                                                        </PXGridColumn>
                                                    </AddItem>

                                                    <!-- Col 4: Unit Price (currency format) -->
                                                    <AddItem>
                                                        <PXGridColumn TypeFullName="PX.Web.UI.PXGridColumn">
                                                            <Prop Key="DataField"   Value="UnitPrice" />
                                                            <Prop Key="Width"       Value="90px" />
                                                            <Prop Key="TextAlign"   Value="Right" />
                                                            <Prop Key="HeaderText"  Value="Unit Price" />
                                                            <Prop Key="Format"      Value="Currency" />
                                                        </PXGridColumn>
                                                    </AddItem>

                                                    <!-- Col 5: UOM -->
                                                    <AddItem>
                                                        <PXGridColumn TypeFullName="PX.Web.UI.PXGridColumn">
                                                            <Prop Key="DataField"   Value="UOM" />
                                                            <Prop Key="Width"       Value="60px" />
                                                            <Prop Key="HeaderText"  Value="UOM" />
                                                        </PXGridColumn>
                                                    </AddItem>

                                                </Children>
                                            </PXGridLevel>
                                        </AddItem>
                                    </Children>

                                </PXGrid>
                            </AddItem>

                        </Children>
                    </PXGroupBox>
                </AddItem>

            </Children>
        </PXSmartPanel>
        <!-- ══ End Break Pricing Panel ════════════════════════════════════════ -->

    </Page>

</Customization>
